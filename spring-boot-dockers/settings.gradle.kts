// ############################################################################
// settings.gradle.kts — "이 빌드에 어떤 프로젝트가 포함되는가" 를 정의하는 파일
// ----------------------------------------------------------------------------
// build.gradle.kts 와 헷갈리기 쉬운데 역할이 다르다.
//   settings.gradle.kts : 빌드의 "구성원" 을 정한다 (프로젝트 이름, 하위 모듈 목록)
//   build.gradle.kts    : 각 프로젝트를 "어떻게 빌드할지" 정한다 (의존성, 플러그인)
//
// gradle 은 빌드를 시작할 때 이 파일을 가장 먼저 읽는다.
// 그래서 이 파일이 있는 위치가 곧 프로젝트의 루트로 인식된다.
// ############################################################################

// 프로젝트 이름. 빌드 결과물 이름의 기본값이 된다.
// 예: 이 이름 + version -> spring-boot-dockers-0.0.1-SNAPSHOT.jar
rootProject.name = "spring-boot-dockers"

// ----------------------------------------------------------------------------
// 참고: 멀티 모듈 프로젝트로 키우고 싶다면 여기에 include 를 추가한다.
//
//   include("api")
//   include("core")
//
// 그러면 api/, core/ 폴더가 각각 하위 프로젝트가 되고,
// 폴더마다 자기 build.gradle.kts 를 갖게 된다.
//
// 이 예제는 단일 모듈이므로 rootProject.name 만 있으면 충분하다.
// ----------------------------------------------------------------------------
